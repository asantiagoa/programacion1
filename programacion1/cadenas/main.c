#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main()
{
    char cadena[20];
    fflush(stdin);
    fgets(cadena, 20, );
    scanf("%s",cadena);
    printf("%s",cadena);
    return 0;
}
