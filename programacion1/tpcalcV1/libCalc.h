#ifndef LIBCALC_H
#define LIBCALC_H

float calcSum(float x, float y); //se declaran las funciones a usar
float calcRes(float x, float y);
float calcDiv(float x, float y);
float calcMult(float x, float y);
int calcFact(float x);

#endif // LIBCALC_H
